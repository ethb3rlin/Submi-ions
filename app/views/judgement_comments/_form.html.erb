<%= form_for judgement_comment, url: add_comment_judgement_path(judgement_comment.judgement),
    data: {turbo_frame: 'post', controller: 'reset-form', action: 'turbo:submit-end->reset-form#reset' } do |form| %>
  <div class="field">
    <%= form.text_area :text, class: "textarea" %>
  </div>

  <div class="field">
    <%= form.submit "Post", class: "button is-primary" %>
  </div>
<% end %>
