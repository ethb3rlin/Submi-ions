<p style="color: green"><%= notice %></p>

<%= render @submission %>

<div>
  <%= link_to "Edit this submission", edit_submission_path(@submission) if policy(@submission).update? %> |
  <%= link_to "Back to submissions", submissions_path %>

  <%= button_to "Destroy this submission", @submission, method: :delete if policy(@submission).destroy? %>
</div>
